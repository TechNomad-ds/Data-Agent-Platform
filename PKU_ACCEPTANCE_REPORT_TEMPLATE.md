# 北大平台课程问答验收报告模板

验收日期：

验收人：

部署地址：

后端版本 / Commit：

测试账号：

## 1. 自动化检查

| 检查项 | 命令 | 结果 | 备注 |
|---|---|---|---|
| 前端公式/流式隔离回归 | `node scripts/pku_frontend_regression_check.mjs` | 通过 / 失败 |  |
| 基础 API smoke | `backend/venv/bin/python scripts/pku_acceptance_smoke.py --base-url <后端地址>` | 通过 / 失败 | 报告：`eval/pku_acceptance_report.json` |
| 真实 LLM 行为验收 | `backend/venv/bin/python scripts/pku_llm_acceptance_check.py --base-url <后端地址>` | 通过 / 失败 | 报告：`eval/pku_llm_acceptance_report.json` |
| 前端构建 | `cd frontend && npm run build` | 通过 / 失败 |  |

## 2. 测试数据空间

| 数据空间 | 资料类型 | 文件名 | 上传结果 | 处理状态 | 备注 |
|---|---|---|---|---|---|
| 计算机体系结构 | DOCX / PPTX / Markdown / PDF |  | 成功 / 失败 | ready / processing / error |  |
| 线性代数复习 | DOCX / PPTX / CSV / Markdown |  | 成功 / 失败 | ready / processing / error |  |
| 现代文学导论 | DOCX / PPTX / TXT / Markdown |  | 成功 / 失败 | ready / processing / error |  |

截图：

- 数据空间列表：
- 课程空间文件列表：
- 7 文件批量上传成功：

## 3. 功能问题回归

| 原问题 | 操作 | 通过标准 | 结果 | 截图/备注 |
|---|---|---|---|---|
| 输出内容格式乱码 | 让模型输出包含标题、列表、表格、代码块的复习提纲 | 中文、Markdown 表格/列表/代码块显示正常，无明显乱码；列表层级清晰，代码块闭合 | 通过 / 失败 |  |
| 数据空间 name 无法修改 | 将 `计算机体系结构` 改名为 `计算机体系结构-验收` | 列表、详情、聊天顶部均显示新名称 | 通过 / 失败 |  |
| 单次上传 7 个文件请求频繁 | 一次选择 7 个 Markdown/TXT 小文件上传 | 全部上传成功，无 429/请求频繁 | 通过 / 失败 |  |
| 公式渲染问题 | 提问 AMAT、正交投影公式，让模型输出 `$...$` 和 `$$...$$` | 行内公式和块级公式均渲染为数学公式，长公式不撑破布局 | 通过 / 失败 |  |
| 流式输出切换污染 | 在课程空间生成时切到通用对话 | 通用对话不显示课程回答；停止按钮不误停/误写当前通用对话 | 通过 / 失败 |  |

## 4. 穿插问答流程

### 4.1 计算机体系结构

问题 1：

> 请解释流水线冒险、缓存局部性和 CPI 的关系。

通过标准：

- 回答能引用上传资料中的流水线、cache/CPI 信息。
- 能区分结构冒险、数据冒险、控制冒险。
- 能说明 cache miss 会通过停顿周期影响 CPI。
- 结构接近“核心概念 → 易混点 → 例题/应用 → 复习建议”，适合学生复习。

结果：通过 / 失败

截图：

问题 2：

> 我擅长 Python，但没学过硬件，我该如何理解 cache miss？

通过标准：

- 回答用个性化类比帮助 Python 背景学生理解。
- 不只复述定义，能给出学习路径或例子。
- 明确承接“擅长 Python”这个背景，不泛泛回答。

结果：通过 / 失败

截图：

问题 3：

> 用公式说明平均访存时间 AMAT。

通过标准：

- 公式正确：`AMAT = Hit Time + Miss Rate × Miss Penalty`。
- 公式渲染正常。

结果：通过 / 失败

截图：

### 4.2 现代文学导论

问题 1：

> 比较鲁迅和沈从文的叙事风格。

通过标准：

- 能从叙事视角、主题、语言风格或历史语境比较。
- 不把两个作家的风格混淆。

结果：通过 / 失败

截图：

问题 2：

> 如果我熟悉历史背景但文学理论薄弱，应该怎么复习？

通过标准：

- 能给出针对性复习路线。
- 能结合课程资料里的文学理论关键词。
- 如果资料依据不足，能明确说明哪些是通用学习建议。

结果：通过 / 失败

截图：

### 4.3 线性代数复习

问题 1：

> 特征值和特征向量的几何意义是什么？

通过标准：

- 能说明方向不变、尺度变化。
- 能配合线性变换理解，而不是只给代数定义。

结果：通过 / 失败

截图：

问题 2：

> 我会解方程组，但没理解线性变换，怎么理解矩阵？

通过标准：

- 能从方程组过渡到列空间/线性映射/坐标变换。
- 能给出学习建议。

结果：通过 / 失败

截图：

问题 3：

> 把正交投影的公式用 LaTeX 写出来。

通过标准：

- 公式正确，LaTeX 正常渲染。
- 能解释公式含义。

结果：通过 / 失败

截图：

### 4.4 满意度/评论分析补充

问题：

> 算整体满意度和推荐率；把评论做情感分类和主题归纳，特别注意“希望增加更多实战”这类句子应该算作改进诉求还是正面评价。

通过标准：

- 不只靠关键词判断情感，能结合评分、推荐意愿和完整句意。
- 能把“希望增加更多实战/多放案例/答疑更快”等句子识别为改进诉求或建设性建议，而不是因为包含“实战/案例”就判为正面。
- 每类情感或主题能给出代表性原文短句/样例，并说明不确定性或分类口径。

结果：通过 / 失败

截图：

## 5. 回头追问记忆/上下文

回到 `计算机体系结构-验收` 后提问：

> 刚才我们说 cache miss 会影响哪个指标？结合 AMAT 再解释一次。

通过标准：

- 能回答 cache miss 影响 AMAT 和 CPI/停顿周期。
- 能保持当前课程空间上下文，不混入文学或线代资料。

结果：通过 / 失败

截图：

## 6. 宣传素材候选

| 素材 | 文件名/截图位置 | 可用性 | 备注 |
|---|---|---|---|
| 多课程空间列表 |  | 可用 / 不可用 |  |
| 多格式资料上传 |  | 可用 / 不可用 |  |
| 个性化学习解释 |  | 可用 / 不可用 |  |
| 公式渲染 |  | 可用 / 不可用 |  |
| 回头追问 |  | 可用 / 不可用 |  |

## 7. 问题记录

| 时间 | 数据空间 | 文件/问题 | 现象 | 严重级别 | 复现步骤 | 后端日志 | 处理建议 |
|---|---|---|---|---|---|---|---|
|  |  |  |  | P0/P1/P2/P3 |  |  |  |

严重级别建议：

- P0：无法登录、无法上传、无法对话、数据越权。
- P1：核心问答明显错误、流式污染、公式/中文大面积不可读。
- P2：单个格式处理失败、个别 UI 异常、回答质量不稳定。
- P3：文案、样式、截图素材优化。

## 8. 结论

整体结果：通过 / 有条件通过 / 不通过

上线前必须修复：

上线后观察项：

宣传素材是否可用：是 / 否
